const config = {
    // Update this if you aren't using the default backend host and port
    // backend_url: 'http://flip1.engr.oregonstate.edu:3123/api'
    backend_url: '/api'
};

export default config;